#include "monty.h"
/**
 * _nop - does nothing but doesnt exit
 *
 * @stack: integer stack
 * @line_number: file's line number
 */
void _nop(stack_t **stack, unsigned int line_number)
{
(void)stack;
(void)line_number;
}
